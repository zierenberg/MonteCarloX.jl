using Plots
default(
    background_color         = :transparent,
    background_color_outside = :transparent,
    dpi                      = 150,
    grid                     = false,
    margin                   = 5Plots.mm,
)